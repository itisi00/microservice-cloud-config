
spring:
  profiles:
    active: dev # 激活开发环境配置
---
# 开发环境
server:
  port: 4001
spring:
  profiles: dev
  application:
    name: microservice-config-dev
---
# 生产环境
server:
  port: 4002
spring:
  profiles: prod
    application:
     name: microservice-config-prod

# 测试环境

